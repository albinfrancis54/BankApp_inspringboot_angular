import { AdminTransationPipe } from './admin-transation.pipe';

describe('AdminTransationPipe', () => {
  it('create an instance', () => {
    const pipe = new AdminTransationPipe();
    expect(pipe).toBeTruthy();
  });
});
