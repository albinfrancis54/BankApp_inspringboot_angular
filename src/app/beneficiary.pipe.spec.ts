import { BeneficiaryPipe } from './beneficiary.pipe';

describe('BeneficiaryPipe', () => {
  it('create an instance', () => {
    const pipe = new BeneficiaryPipe();
    expect(pipe).toBeTruthy();
  });
});
